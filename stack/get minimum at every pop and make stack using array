******ques link *******       https://practice.geeksforgeeks.org/problems/get-min-at-pop/1


*****we will push min eleement every time so wwe will havev max at top*************
stack<int>_push(int arr[],int n)
{
   // your code here
   stack<int>s;
   int max1 = INT_MAX;
   for(int i =0;i<n;i++){
       max1 = min(arr[i],max1);
       s.push(max1);
   }
   return s;
}
